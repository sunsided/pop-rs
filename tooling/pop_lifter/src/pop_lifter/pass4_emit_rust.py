"""Pass 4: emit Rust source from IR3.

TODO:
- One Rust module per upstream `.S` (snake_cased).
- Function names preserved verbatim (`do_strike`, `check_floor`, ...).
- Global state lives on `struct Game`; routines are
  `impl<R: Renderer, A: Audio, I: Input> Game<R, A, I> { ... }`.
- Generated files carry `// @generated by pop_lifter <git-sha>` and a
  source-line manifest linking back to `vendor/pop-apple2/...`.
- Run `cargo fmt` + `clippy --deny warnings` at the end.
"""

from __future__ import annotations
