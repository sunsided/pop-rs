// @generated by pop_lifter — DO NOT EDIT.
//
// Pass 4 skeleton slice: module + routine scaffolding with leaf-
// expression, control-flow, data-movement, carry-arithmetic,
// `(ptr),y` indirect, cmp/bit flag, and 16-bit (`Wide16`) lowering.
// Flags are `self.c`/`self.z`/`self.n: u8` (provisional). SMC, the
// stack, `RawIfStmt`, and `GotoStmt`/`LabelStmt` are deferred; they
// appear as `// raw: …` or `// TODO(pass4): …` comments.
// The `Cpu` receiver and `self.ram`/`self.c`/`self.z`/`self.n` are
// provisional, pending the state/trait design slice. RAM addresses
// keep their source symbol names via the `sym` constants below.
//
// source: 01 POP Source/Source/COLL.S

#[allow(non_upper_case_globals)]
mod sym {
    pub const AMtimer: usize = 0x00c7;
    pub const BarL: usize = 0x007b;
    pub const BarR: usize = 0x007c;
    pub const BlockYlast: usize = 0x039b;
    pub const BlockYthis: usize = 0x039a;
    pub const BlueSpec: usize = 0x0026;
    pub const CDLeftEj: usize = 0x00be;
    pub const CDRightEj: usize = 0x00bf;
    pub const CDbelow: usize = 0x0340;
    pub const CDlastframe: usize = 0x0330;
    pub const CDthisframe: usize = 0x0320;
    pub const CharAction: usize = 0x0046;
    pub const CharBlockX: usize = 0x0044;
    pub const CharBlockY: usize = 0x0045;
    pub const CharFace: usize = 0x0043;
    pub const CharID: usize = 0x004d;
    pub const CharLife: usize = 0x004f;
    pub const CharPosn: usize = 0x0040;
    pub const CharScrn: usize = 0x004b;
    pub const CharSeq: usize = 0x0049;
    pub const CharSword: usize = 0x004e;
    pub const CharX: usize = 0x0041;
    pub const CharXVel: usize = 0x0047;
    pub const CharY: usize = 0x0042;
    pub const CharYVel: usize = 0x0048;
    pub const CollFace: usize = 0x00f1;
    pub const FloorY: usize = 0x127e;
    pub const NextLevel: usize = 0x031c;
    pub const SNabove: usize = 0x0390;
    pub const SNbelow: usize = 0x0380;
    pub const SNlastframe: usize = 0x0370;
    pub const SNthisframe: usize = 0x0360;
    pub const VisScrn: usize = 0x00cb;
    pub const alertguard: usize = 0x00e4;
    pub const begrange: usize = 0x00c8;
    pub const blockedge: usize = 0x00c2;
    pub const blocky: usize = 0x008d;
    pub const bufindex: usize = 0x00c1;
    pub const collX: usize = 0x0087;
    pub const collideL: usize = 0x00c3;
    pub const collideR: usize = 0x00c4;
    pub const createshad: usize = 0x00e5;
    pub const endrange: usize = 0x00c0;
    pub const imheight: usize = 0x00b5;
    pub const infrontx: usize = 0x008e;
    pub const jarabove: usize = 0x00cd;
    pub const level: usize = 0x03f4;
    pub const scrnBelowL: usize = 0x0035;
    pub const scrnBelowR: usize = 0x0038;
    pub const scrnLeft: usize = 0x0031;
    pub const scrnRight: usize = 0x0032;
    pub const tempblockx: usize = 0x0091;
    pub const tempblocky: usize = 0x0092;
    pub const tempobjid: usize = 0x00f2;
    pub const tempscrn: usize = 0x0093;
    pub const weightless: usize = 0x00c5;
    pub const ztemp: usize = 0x00e9;
}

impl Cpu {
    fn CHECKBARR(&mut self) {
        self.ram[sym::collideL] = 0xff;
        self.ram[sym::collideR] = 0xff;
        self.a = self.ram[sym::CharAction];
        let _o: u8 = 0x07;
        self.c = (self.a >= _o) as u8;
        self.z = (self.a == _o) as u8;
        self.n = self.a.wrapping_sub(_o) >> 7;
        if self.a == 0x07 {
        } else {
            self.ram[sym::BlockYthis] = self.ram[sym::CharBlockY];
            self.initCDbufs();
            self.ram[sym::BlockYlast] = self.ram[sym::BlockYthis];
            self.a = self.ram[sym::CDRightEj];
            self.getblockxp();
            self.c = 0;
            let _r = (self.a as u16) + (0x02) as u16 + (self.c as u16);
            self.a = _r as u8;
            self.c = (_r >> 8) as u8;
            let _o: u8 = 0x0b;
            self.c = (self.a >= _o) as u8;
            self.z = (self.a == _o) as u8;
            self.n = self.a.wrapping_sub(_o) >> 7;
            if self.a < 0x0b {
            } else {
                self.a = 0x0b;
            }
            self.ram[sym::endrange] = self.a;
            self.a = self.ram[sym::CDLeftEj];
            self.getblockxp();
            self.x = self.a;
            self.x = self.x.wrapping_sub(1);
            self.ram[sym::begrange] = self.x;
            self.ram[sym::blocky] = self.ram[sym::BlockYthis];
            self.a = 0x60;
            self.x = 0x20;
            self.getCData();
            self.a = self.ram[sym::BlockYthis];
            self.c = 0;
            let _r = (self.a as u16) + (0x01) as u16 + (self.c as u16);
            self.a = _r as u8;
            self.c = (_r >> 8) as u8;
            self.ram[sym::blocky] = self.a;
            self.a = 0x80;
            self.x = 0x40;
            self.getCData();
            self.a = self.ram[sym::BlockYthis];
            self.c = 1;
            let _r = (self.a as u16) + (!0x01_u8) as u16 + (self.c as u16);
            self.a = _r as u8;
            self.c = (_r >> 8) as u8;
            self.ram[sym::blocky] = self.a;
            self.a = 0x90;
            self.x = 0x50;
            self.getCData();
            self.x = 0x09;
            loop {
                self.a = self.ram[sym::SNthisframe + self.x as usize];
                if (self.a as i8) < 0 {
                } else {
                    let _o: u8 = self.ram[sym::SNlastframe + self.x as usize];
                    self.c = (self.a >= _o) as u8;
                    self.z = (self.a == _o) as u8;
                    self.n = self.a.wrapping_sub(_o) >> 7;
                    if self.a != self.ram[sym::SNlastframe + self.x as usize] {
                    } else {
                        self.a = self.ram[sym::CDlastframe + self.x as usize];
                        self.a &= 0x0f;
                        if self.a != 0x00 {
                        } else {
                            self.a = self.ram[sym::CDthisframe + self.x as usize];
                            self.a &= 0x0f;
                            if self.a == 0x00 {
                            } else {
                                self.ram[sym::collideL] = self.x;
                            }
                        }
                        self.a = self.ram[sym::CDlastframe + self.x as usize];
                        self.a &= 0xf0;
                        if self.a != 0x00 {
                        } else {
                            self.a = self.ram[sym::CDthisframe + self.x as usize];
                            self.a &= 0xf0;
                            if self.a == 0x00 {
                            } else {
                                self.ram[sym::collideR] = self.x;
                            }
                        }
                    }
                }
                self.x = self.x.wrapping_sub(0x01);
                if !((self.x as i8) >= 0) {
                    break;
                }
            }
            self.x = self.ram[sym::collideL];
            self.y = self.ram[sym::collideR];
        }
        return;
    }

    fn getCData(&mut self) {
        // raw: patch *:smodSN+1 = a            ; COLL.S:195
        // raw: patch *:smodCD+1 = x            ; COLL.S:196
        self.a = self.ram[sym::begrange];
        self.getblockej();
        self.c = 0;
        let _r = (self.a as u16) + (0x07) as u16 + (self.c as u16);
        self.a = _r as u8;
        self.c = (_r >> 8) as u8;
        self.ram[sym::blockedge] = self.a;
        self.x = self.ram[sym::begrange];
        loop {
            self.ram[sym::bufindex] = self.x;
            self.a = self.ram[sym::CharScrn];
            self.x = self.ram[sym::bufindex];
            self.y = self.ram[sym::blocky];
            self.getleftbar();
            let _o: u8 = self.ram[sym::CDRightEj];
            self.c = (self.a >= _o) as u8;
            self.z = (self.a == _o) as u8;
            self.n = self.a.wrapping_sub(_o) >> 7;
            if self.a < self.ram[sym::CDRightEj] {
                self.a = 0x0f;
            } else {
                self.a = 0x00;
                if self.a == 0x00 {
                } else {
                    self.a = 0x0f;
                }
            }
            self.ram[sym::ztemp] = self.a;
            self.a = self.ram[sym::CharScrn];
            self.x = self.ram[sym::bufindex];
            self.y = self.ram[sym::blocky];
            self.getrightbar();
            let _o: u8 = self.ram[sym::CDLeftEj];
            self.c = (self.a >= _o) as u8;
            self.z = (self.a == _o) as u8;
            self.n = self.a.wrapping_sub(_o) >> 7;
            if self.a < self.ram[sym::CDLeftEj] {
                self.a = 0x00;
            } else {
                // TODO(pass4): lower RawIfStmt
            }
            self.a |= self.ram[sym::ztemp];
            self.x = self.ram[sym::tempblockx];
            self.ram[sym::CDthisframe + self.x as usize] = self.a;
            self.ram[sym::SNthisframe + self.x as usize] = self.ram[sym::tempscrn];
            self.a = self.ram[sym::blockedge];
            self.c = 0;
            let _r = (self.a as u16) + (0x0e) as u16 + (self.c as u16);
            self.a = _r as u8;
            self.c = (_r >> 8) as u8;
            self.ram[sym::blockedge] = self.a;
            self.x = self.ram[sym::bufindex];
            self.x = self.x.wrapping_add(1);
            let _o: u8 = self.ram[sym::endrange];
            self.c = (self.x >= _o) as u8;
            self.z = (self.x == _o) as u8;
            self.n = self.x.wrapping_sub(_o) >> 7;
            if !(self.x != self.ram[sym::endrange]) {
                break;
            }
        }
        return;
    }

    fn initCDbufs(&mut self) {
        self.a = self.ram[sym::BlockYthis];
        let _o: u8 = self.ram[sym::BlockYlast];
        self.c = (self.a >= _o) as u8;
        self.z = (self.a == _o) as u8;
        self.n = self.a.wrapping_sub(_o) >> 7;
        if self.a == self.ram[sym::BlockYlast] {
            self.a = 0x60;
            self.x = 0x20;
        } else {
            self.c = 0;
            let _r = (self.a as u16) + (0x03) as u16 + (self.c as u16);
            self.a = _r as u8;
            self.c = (_r >> 8) as u8;
            let _o: u8 = self.ram[sym::BlockYlast];
            self.c = (self.a >= _o) as u8;
            self.z = (self.a == _o) as u8;
            self.n = self.a.wrapping_sub(_o) >> 7;
            if self.a == self.ram[sym::BlockYlast] {
                self.a = 0x60;
                self.x = 0x20;
            } else {
                self.c = 1;
                let _r = (self.a as u16) + (!0x06_u8) as u16 + (self.c as u16);
                self.a = _r as u8;
                self.c = (_r >> 8) as u8;
                let _o: u8 = self.ram[sym::BlockYlast];
                self.c = (self.a >= _o) as u8;
                self.z = (self.a == _o) as u8;
                self.n = self.a.wrapping_sub(_o) >> 7;
                if self.a == self.ram[sym::BlockYlast] {
                    self.a = 0x60;
                    self.x = 0x20;
                } else {
                    self.a = self.ram[sym::BlockYthis];
                    self.c = 0;
                    let _r = (self.a as u16) + (0x01) as u16 + (self.c as u16);
                    self.a = _r as u8;
                    self.c = (_r >> 8) as u8;
                    let _o: u8 = self.ram[sym::BlockYlast];
                    self.c = (self.a >= _o) as u8;
                    self.z = (self.a == _o) as u8;
                    self.n = self.a.wrapping_sub(_o) >> 7;
                    if self.a == self.ram[sym::BlockYlast] {
                        self.a = 0x90;
                        self.x = 0x50;
                    } else {
                        self.c = 1;
                        let _r = (self.a as u16) + (!0x03_u8) as u16 + (self.c as u16);
                        self.a = _r as u8;
                        self.c = (_r >> 8) as u8;
                        let _o: u8 = self.ram[sym::BlockYlast];
                        self.c = (self.a >= _o) as u8;
                        self.z = (self.a == _o) as u8;
                        self.n = self.a.wrapping_sub(_o) >> 7;
                        if self.a == self.ram[sym::BlockYlast] {
                            self.a = 0x90;
                            self.x = 0x50;
                        } else {
                            self.a = 0x80;
                            self.x = 0x40;
                        }
                    }
                }
            }
        }
        // raw: patch *:smodSN+1 = a            ; COLL.S:306
        // raw: patch *:smodCD+1 = x            ; COLL.S:307
        self.x = 0x09;
        loop {
            self.ram[sym::SNlastframe + self.x as usize] = self.ram[sym::SNbelow + self.x as usize];
            self.ram[sym::CDlastframe + self.x as usize] = self.ram[sym::CDbelow + self.x as usize];
            self.a = 0xff;
            self.ram[sym::SNthisframe + self.x as usize] = self.a;
            self.ram[sym::SNabove + self.x as usize] = self.a;
            self.ram[sym::SNbelow + self.x as usize] = self.a;
            self.x = self.x.wrapping_sub(0x01);
            if !((self.x as i8) >= 0) {
                break;
            }
        }
        return;
    }

    fn getleftbar(&mut self) {
        self.rdblock();
        self.cmpbarr();
        // TODO(pass4): lower RawIfStmt
        self.y = self.a;
        self.a = self.ram[sym::blockedge];
        self.c = 0;
        let _r = (self.a as u16) + self.ram[sym::BarL + self.y as usize] as u16 + (self.c as u16);
        self.a = _r as u8;
        self.c = (_r >> 8) as u8;
        return;
    }

    fn getrightbar(&mut self) {
        self.rdblock();
        self.cmpbarr();
        // TODO(pass4): lower RawIfStmt
        self.y = self.a;
        self.a = self.ram[sym::blockedge];
        self.c = 0;
        let _r = (self.a as u16) + (0x0d) as u16 + (self.c as u16);
        self.a = _r as u8;
        self.c = (_r >> 8) as u8;
        self.c = 1;
        let _r = (self.a as u16) + (!self.ram[sym::BarR + self.y as usize]) as u16 + (self.c as u16);
        self.a = _r as u8;
        self.c = (_r >> 8) as u8;
        return;
    }

    fn COLLISIONS(&mut self) {
        self.a = self.ram[sym::AMtimer];
        if self.a == 0x00 {
            self.a = self.ram[sym::CharAction];
            if self.a == 0x02 {
                return;
            }
            if self.a == 0x06 {
                return;
            }
            self.a = self.ram[sym::CharPosn];
            if self.a >= 0x87 {
                if self.a < 0x95 {
                    return;
                }
            }
            self.x = self.ram[sym::collideL];
            if (self.x as i8) < 0 {
                self.x = self.ram[sym::collideR];
                if (self.x as i8) < 0 {
                    return;
                }
                self.ram[sym::collX] = self.x;
                self.rightcoll();
                return;
            }
            self.ram[sym::collX] = self.x;
            self.leftcoll();
            return;
        }
        self.a = self.ram[0xc030];
        self.ram[sym::AMtimer] = self.ram[sym::AMtimer].wrapping_sub(1);
        return;
    }

    fn rightcoll(&mut self) {
        self.a = self.ram[sym::CharSword];
        if self.a != 0x02 {
            self.a = self.ram[sym::CharFace];
            if (self.a as i8) >= 0 {
                return;
            }
        }
        self.checkcoll1();
        // TODO(pass4): lower RawIfStmt
        self.a = self.ram[sym::tempscrn];
        self.x = self.ram[sym::tempblockx];
        self.y = self.ram[sym::tempblocky];
        self.getrightbar();
        self.c = 1;
        let _r = (self.a as u16) + (!self.ram[sym::CDLeftEj]) as u16 + (self.c as u16);
        self.a = _r as u8;
        self.c = (_r >> 8) as u8;
        self.x = 0x00;
        self.collide();
        return;
    }

    fn leftcoll(&mut self) {
        self.a = self.ram[sym::CharSword];
        if self.a != 0x02 {
            self.a = self.ram[sym::CharFace];
            if self.a != 0x00 {
                return;
            }
        }
        self.checkcoll1();
        // TODO(pass4): lower RawIfStmt
        self.a = self.ram[sym::tempscrn];
        self.x = self.ram[sym::tempblockx];
        self.y = self.ram[sym::tempblocky];
        self.getleftbar();
        self.c = 1;
        let _r = (self.a as u16) + (!self.ram[sym::CDRightEj]) as u16 + (self.c as u16);
        self.a = _r as u8;
        self.c = (_r >> 8) as u8;
        self.x = 0xff;
        self.collide();
        return;
    }

    fn checkcoll1(&mut self) {
        'b4: {
            self.ram[sym::tempblockx] = self.x;
            self.a = self.ram[sym::CharBlockY];
            if (self.a as i8) < 0 {
                self.c = 0;
                let _r = (self.a as u16) + (0x03) as u16 + (self.c as u16);
                self.a = _r as u8;
                self.c = (_r >> 8) as u8;
                // TODO(pass4): lower RawIfStmt
            }
            if self.a >= 0x03 {
                self.c = 1;
                let _r = (self.a as u16) + (!0x03_u8) as u16 + (self.c as u16);
                self.a = _r as u8;
                self.c = (_r >> 8) as u8;
            }
        }
        self.ram[sym::tempblocky] = self.a;
        self.a = self.ram[sym::SNthisframe + self.x as usize];
        self.ram[sym::tempscrn] = self.a;
        self.rdblock1();
        self.CHECKCOLL();
        return;
    }

    fn CHECKCOLL(&mut self) {
        'b13: {
            if self.a != 0x0a {
                'b14: {
                    if self.a != 0x04 {
                        if self.a == 0x12 {
                            self.a = self.ram[(self.ram[sym::BlueSpec] as usize | (self.ram[sym::BlueSpec + 1] as usize) << 8) + self.y as usize];
                            let _o: u8 = 0x02;
                            self.c = (self.a >= _o) as u8;
                            self.z = (self.a == _o) as u8;
                            self.n = self.a.wrapping_sub(_o) >> 7;
                            if self.a != 0x02 {
                                break 'b13;
                            } else {
                                // TODO(pass4): lower RawIfStmt
                            }
                        } else {
                            let _o: u8 = 0x0d;
                            self.c = (self.a >= _o) as u8;
                            self.z = (self.a == _o) as u8;
                            self.n = self.a.wrapping_sub(_o) >> 7;
                            if self.a != 0x0d {
                                // TODO(pass4): lower RawIfStmt
                            }
                            self.a = self.ram[sym::CharID];
                            if self.a != 0x00 {
                                break 'b14;
                            } else {
                                self.a = self.ram[sym::CharPosn];
                                if self.a < 0x27 {
                                    break 'b14;
                                } else {
                                    if self.a >= 0x2c {
                                        break 'b14;
                                    } else {
                                        self.a = self.ram[sym::CharFace];
                                        if (self.a as i8) >= 0 {
                                            break 'b14;
                                        } else {
                                            self.smashmirror();
                                            self.a = 0xff;
                                            self.ram[sym::createshad] = self.a;
                                            self.c = 0;
                                            return;
                                        }
                                    }
                                }
                            }
                        }
                    }
                    self.gatebarr?();
                    // TODO(pass4): lower RawIfStmt
                }
                self.a = self.ram[sym::tempblockx];
                self.getblockej();
                self.AdjustScrn();
                self.c = 0;
                let _r = (self.a as u16) + (0x07) as u16 + (self.c as u16);
                self.a = _r as u8;
                self.c = (_r >> 8) as u8;
                self.ram[sym::blockedge] = self.a;
                self.c = 1;
                return;
            }
        }
        self.c = 0;
        return;
    }

    fn AdjustScrn(&mut self) {
        self.x = self.ram[sym::tempscrn];
        if self.x == self.ram[sym::VisScrn] {
            return;
        }
        if self.x != self.ram[sym::scrnLeft] {
            if self.x != self.ram[sym::scrnBelowL] {
                if self.x != self.ram[sym::scrnRight] {
                    if self.x != self.ram[sym::scrnBelowR] {
                        return;
                    }
                }
                self.c = 0;
                let _r = (self.a as u16) + (0x8c) as u16 + (self.c as u16);
                self.a = _r as u8;
                self.c = (_r >> 8) as u8;
                return;
            }
        }
        self.c = 1;
        let _r = (self.a as u16) + (!0x8c_u8) as u16 + (self.c as u16);
        self.a = _r as u8;
        self.c = (_r >> 8) as u8;
        return;
    }

    fn collide(&mut self) {
        self.ram[sym::CollFace] = self.x;
        self.x = self.ram[sym::CharLife];
        if (self.x as i8) >= 0 {
            return;
        }
        self.x = self.ram[sym::CharPosn];
        if self.x == 0xb1 {
            return;
        }
        'b17: {
            'b16: {
                'b13: {
                    self.c = 0;
                    let _r = (self.a as u16) + self.ram[sym::CharX] as u16 + (self.c as u16);
                    self.a = _r as u8;
                    self.c = (_r >> 8) as u8;
                    self.ram[sym::CharX] = self.a;
                    self.rdblock1();
                    self.x = self.ram[sym::CollFace];
                    if (self.x as i8) >= 0 {
                        if self.a != 0x0c {
                            if self.a != 0x07 {
                                if self.a != 0x14 {
                                    break 'b13;
                                }
                            }
                        }
                        self.ram[sym::tempblockx] = self.ram[sym::tempblockx].wrapping_add(1);
                        self.a = self.ram[sym::tempscrn];
                        if self.a != 0x00 {
                            break 'b16;
                        } else {
                            self.a = self.ram[sym::tempblockx];
                            if self.a != 0x0a {
                                break 'b16;
                            } else {
                                self.a = self.ram[sym::CharScrn];
                                self.ram[sym::tempscrn] = self.a;
                                self.a = 0x00;
                                self.ram[sym::tempblockx] = self.a;
                                break 'b16;
                            }
                        }
                    } else {
                        let _o: u8 = 0x14;
                        self.c = (self.a >= _o) as u8;
                        self.z = (self.a == _o) as u8;
                        self.n = self.a.wrapping_sub(_o) >> 7;
                        if self.a != 0x14 {
                            // TODO(pass4): lower RawIfStmt
                        }
                        self.ram[sym::tempblockx] = self.ram[sym::tempblockx].wrapping_sub(1);
                        break 'b16;
                    }
                }
                self.cmpspace();
                // TODO(pass4): lower RawIfStmt
                self.a = 0xfc;
                self.addcharx();
                self.ram[sym::CharX] = self.a;
                self.a = self.ram[sym::CharAction];
                if self.a == 0x04 {
                    self.a = 0x00;
                    self.ram[sym::CharXVel] = self.a;
                    // TODO(pass4): lower RawIfStmt
                }
            }
            self.a = 0x2d;
            self.jumpseq();
            self.animchar();
        }
        self.ram[sym::alertguard] = 0x01;
        self.a = 0x0d;
        self.addsound();
        return;
    }

    fn AirBump(&mut self) {
        'b3: {
            self.a = 0xfc;
            self.addcharx();
            self.ram[sym::CharX] = self.a;
            self.a = self.ram[sym::CharAction];
            if self.a == 0x04 {
                self.a = 0x00;
                self.ram[sym::CharXVel] = self.a;
                // TODO(pass4): lower RawIfStmt
            }
            self.a = 0x2d;
            self.jumpseq();
            self.animchar();
        }
        self.ram[sym::alertguard] = 0x01;
        self.a = 0x0d;
        self.addsound();
        return;
    }

    // aliases: BumpSound
    fn :smackwall(&mut self) {
        self.ram[sym::alertguard] = 0x01;
        self.a = 0x0d;
        self.addsound();
        return;
    }

    fn GroundBump(&mut self) {
        self.x = self.ram[sym::CharBlockY];
        self.a = self.ram[sym::CharSword];
        let _o: u8 = 0x02;
        self.c = (self.a >= _o) as u8;
        self.z = (self.a == _o) as u8;
        self.n = self.a.wrapping_sub(_o) >> 7;
        if self.a != 0x02 {
            self.a = self.ram[sym::FloorY + 1 + self.x as usize];
            self.c = 1;
            let _r = (self.a as u16) + (!self.ram[sym::CharY]) as u16 + (self.c as u16);
            self.a = _r as u8;
            self.c = (_r >> 8) as u8;
            let _o: u8 = 0x0f;
            self.c = (self.a >= _o) as u8;
            self.z = (self.a == _o) as u8;
            self.n = self.a.wrapping_sub(_o) >> 7;
            if self.a >= 0x0f {
                self.AirBump();
                return;
            }
        }
        self.ram[sym::CharY] = self.ram[sym::FloorY + 1 + self.x as usize];
        self.a = self.ram[sym::CharYVel];
        let _o: u8 = 0x16;
        self.c = (self.a >= _o) as u8;
        self.z = (self.a == _o) as u8;
        self.n = self.a.wrapping_sub(_o) >> 7;
        if self.a < 0x16 {
            self.ram[sym::CharYVel] = 0x00;
            self.a = self.ram[sym::CharLife];
            if self.a == 0x00 {
                return;
            }
            'b15: {
                self.a = self.ram[sym::CharSword];
                let _o: u8 = 0x02;
                self.c = (self.a >= _o) as u8;
                self.z = (self.a == _o) as u8;
                self.n = self.a.wrapping_sub(_o) >> 7;
                if self.a == 0x02 {
                    self.a = self.ram[sym::CollFace];
                    let _o: u8 = self.ram[sym::CharFace];
                    self.c = (self.a >= _o) as u8;
                    self.z = (self.a == _o) as u8;
                    self.n = self.a.wrapping_sub(_o) >> 7;
                    if self.a != self.ram[sym::CharFace] {
                        self.a = 0x40;
                        if self.a != 0x00 {
                            break 'b15;
                        }
                    }
                    self.a = 0x41;
                    self.jumpseq();
                    self.animchar();
                    self.a = 0x01;
                    self.addcharx();
                    self.ram[sym::CharX] = self.a;
                    return;
                }
                'b14: {
                    self.x = self.ram[sym::CharPosn];
                    let _o: u8 = 0x18;
                    self.c = (self.x >= _o) as u8;
                    self.z = (self.x == _o) as u8;
                    self.n = self.x.wrapping_sub(_o) >> 7;
                    if self.x != 0x18 {
                        let _o: u8 = 0x19;
                        self.c = (self.x >= _o) as u8;
                        self.z = (self.x == _o) as u8;
                        self.n = self.x.wrapping_sub(_o) >> 7;
                        if self.x != 0x19 {
                            let _o: u8 = 0x28;
                            self.c = (self.x >= _o) as u8;
                            self.z = (self.x == _o) as u8;
                            self.n = self.x.wrapping_sub(_o) >> 7;
                            if self.x >= 0x28 {
                                let _o: u8 = 0x2b;
                                self.c = (self.x >= _o) as u8;
                                self.z = (self.x == _o) as u8;
                                self.n = self.x.wrapping_sub(_o) >> 7;
                                if self.x < 0x2b {
                                    break 'b14;
                                }
                            }
                            let _o: u8 = 0x66;
                            self.c = (self.x >= _o) as u8;
                            self.z = (self.x == _o) as u8;
                            self.n = self.x.wrapping_sub(_o) >> 7;
                            if self.x >= 0x66 {
                                let _o: u8 = 0x6b;
                                self.c = (self.x >= _o) as u8;
                                self.z = (self.x == _o) as u8;
                                self.n = self.x.wrapping_sub(_o) >> 7;
                                if self.x < 0x6b {
                                    break 'b14;
                                }
                            }
                            self.a = 0x2f;
                            self.jumpseq();
                            self.BumpSound();
                            self.animchar();
                            return;
                        }
                    }
                }
                self.a = 0x2e;
            }
            self.jumpseq();
            self.animchar();
            self.BumpSound();
            return;
        }
        self.a = 0xfb;
        self.addcharx();
        self.ram[sym::CharX] = self.a;
        return;
    }

    fn GETFWDDIST(&mut self) {
        self.GetBaseBlock();
        self.setupchar();
        self.getedges();
        self.getunderft();
        self.ram[sym::tempobjid] = self.a;
        self.cmpbarr();
        // TODO(pass4): lower RawIfStmt
        self.a = self.ram[sym::CharBlockX];
        self.ram[sym::tempblockx] = self.a;
        self.DBarr();
        self.x = self.a;
        if (self.x as i8) >= 0 {
            // TODO(pass4): lower GotoStmt
        }
        // TODO(pass4): lower LabelStmt
        self.getinfront();
        self.ram[sym::tempobjid] = self.a;
        let _o: u8 = 0x0c;
        self.c = (self.a >= _o) as u8;
        self.z = (self.a == _o) as u8;
        self.n = self.a.wrapping_sub(_o) >> 7;
        if self.a != 0x0c {
            // TODO(pass4): lower GotoStmt
        }
        self.x = self.ram[sym::CharFace];
        if (self.x as i8) >= 0 {
            // TODO(pass4): lower GotoStmt
        }
        // TODO(pass4): lower LabelStmt
        self.cmpbarr();
        // TODO(pass4): lower RawIfStmt
        self.a = self.ram[sym::infrontx];
        self.ram[sym::tempblockx] = self.a;
        self.DBarr();
        self.x = self.a;
        if (self.x as i8) >= 0 {
            // TODO(pass4): lower GotoStmt
        }
        // TODO(pass4): lower LabelStmt
        self.getinfront();
        self.ram[sym::tempobjid] = self.a;
        let _o: u8 = 0x0b;
        self.c = (self.a >= _o) as u8;
        self.z = (self.a == _o) as u8;
        self.n = self.a.wrapping_sub(_o) >> 7;
        if self.a == 0x0b {
            // TODO(pass4): lower GotoStmt
        }
        let _o: u8 = 0x06;
        self.c = (self.a >= _o) as u8;
        self.z = (self.a == _o) as u8;
        self.n = self.a.wrapping_sub(_o) >> 7;
        if self.a == 0x06 {
            // TODO(pass4): lower GotoStmt
        }
        let _o: u8 = 0x16;
        self.c = (self.a >= _o) as u8;
        self.z = (self.a == _o) as u8;
        self.n = self.a.wrapping_sub(_o) >> 7;
        if self.a == 0x16 {
            // TODO(pass4): lower GotoStmt
        }
        let _o: u8 = 0x0a;
        self.c = (self.a >= _o) as u8;
        self.z = (self.a == _o) as u8;
        self.n = self.a.wrapping_sub(_o) >> 7;
        if self.a == 0x0a {
            // TODO(pass4): lower GotoStmt
        }
        self.cmpspace();
        // TODO(pass4): lower RawIfStmt
        // TODO(pass4): lower LabelStmt
        self.a = 0x0b;
        self.x = 0x02;
        if self.x != 0x00 {
            // TODO(pass4): lower GotoStmt
        }
        // TODO(pass4): lower LabelStmt
        self.getdist();
        // TODO(pass4): lower RawIfStmt
        self.x = 0x00;
        if self.x == 0x00 {
            // TODO(pass4): lower GotoStmt
        }
        // TODO(pass4): lower LabelStmt
        self.getdist();
        self.x = 0x00;
        // TODO(pass4): lower LabelStmt
        self.y = self.ram[sym::tempobjid];
        // TODO(pass4): lower LabelStmt
        return;
        // TODO(pass4): lower LabelStmt
        let _o: u8 = 0x0e;
        self.c = (self.a >= _o) as u8;
        self.z = (self.a == _o) as u8;
        self.n = self.a.wrapping_sub(_o) >> 7;
        if self.a >= 0x0e {
            // TODO(pass4): lower GotoStmt
        }
        self.x = 0x01;
        if self.x != 0x00 {
            // TODO(pass4): lower GotoStmt
        }
        // TODO(pass4): lower LabelStmt
        self.a = self.ram[sym::tempobjid];
        let _o: u8 = 0x04;
        self.c = (self.a >= _o) as u8;
        self.z = (self.a == _o) as u8;
        self.n = self.a.wrapping_sub(_o) >> 7;
        if self.a != 0x04 {
            // TODO(pass4): lower GotoStmt
        }
        self.gatebarr?();
        // TODO(pass4): lower RawIfStmt
        // TODO(pass4): lower LabelStmt
        self.a = self.ram[sym::tempblockx];
        self.getblockej();
        self.c = 0;
        let _r = (self.a as u16) + (0x07) as u16 + (self.c as u16);
        self.a = _r as u8;
        self.c = (_r >> 8) as u8;
        self.ram[sym::blockedge] = self.a;
        self.a = self.ram[sym::CharFace];
        if (self.a as i8) < 0 {
            // TODO(pass4): lower GotoStmt
        }
        // TODO(pass4): lower LabelStmt
        self.a = self.ram[sym::tempobjid];
        self.cmpbarr();
        // TODO(pass4): lower RawIfStmt
        self.y = self.a;
        self.a = self.ram[sym::blockedge];
        self.c = 0;
        let _r = (self.a as u16) + self.ram[sym::BarL + self.y as usize] as u16 + (self.c as u16);
        self.a = _r as u8;
        self.c = (_r >> 8) as u8;
        self.ram[sym::ztemp] = self.a;
        self.c = 1;
        let _r = (self.a as u16) + (!self.ram[sym::CDRightEj]) as u16 + (self.c as u16);
        self.a = _r as u8;
        self.c = (_r >> 8) as u8;
        return;
        // TODO(pass4): lower LabelStmt
        self.a = 0xff;
        return;
        // TODO(pass4): lower LabelStmt
        self.a = self.ram[sym::tempobjid];
        self.cmpbarr();
        // TODO(pass4): lower RawIfStmt
        self.y = self.a;
        self.a = self.ram[sym::blockedge];
        self.c = 0;
        let _r = (self.a as u16) + (0x0d) as u16 + (self.c as u16);
        self.a = _r as u8;
        self.c = (_r >> 8) as u8;
        self.c = 1;
        let _r = (self.a as u16) + (!self.ram[sym::BarR + self.y as usize]) as u16 + (self.c as u16);
        self.a = _r as u8;
        self.c = (_r >> 8) as u8;
        self.ram[sym::ztemp] = self.a;
        self.a = self.ram[sym::CDLeftEj];
        self.c = 1;
        let _r = (self.a as u16) + (!self.ram[sym::ztemp]) as u16 + (self.c as u16);
        self.a = _r as u8;
        self.c = (_r >> 8) as u8;
        // TODO(pass4): lower LabelStmt
        return;
    }

    fn DBarr(&mut self) {
        'b5: {
            self.a = self.ram[sym::tempobjid];
            let _o: u8 = 0x04;
            self.c = (self.a >= _o) as u8;
            self.z = (self.a == _o) as u8;
            self.n = self.a.wrapping_sub(_o) >> 7;
            if self.a == 0x04 {
                self.gatebarr?();
                // TODO(pass4): lower RawIfStmt
            }
            self.a = self.ram[sym::tempblockx];
            self.getblockej();
            self.c = 0;
            let _r = (self.a as u16) + (0x07) as u16 + (self.c as u16);
            self.a = _r as u8;
            self.c = (_r >> 8) as u8;
            self.ram[sym::blockedge] = self.a;
            self.a = self.ram[sym::CharFace];
            if (self.a as i8) < 0 {
                self.a = self.ram[sym::tempobjid];
                self.cmpbarr();
                // TODO(pass4): lower RawIfStmt
            } else {
                self.a = self.ram[sym::tempobjid];
                self.cmpbarr();
                // TODO(pass4): lower RawIfStmt
            }
        }
        self.a = 0xff;
        return;
    }

    // aliases: :next
    fn ANIMCHAR(&mut self) {
        self.getseq();
        let _o: u8 = 0xfb;
        self.c = (self.a >= _o) as u8;
        self.z = (self.a == _o) as u8;
        self.n = self.a.wrapping_sub(_o) >> 7;
        if self.a != 0xfb {
            let _o: u8 = 0xfa;
            self.c = (self.a >= _o) as u8;
            self.z = (self.a == _o) as u8;
            self.n = self.a.wrapping_sub(_o) >> 7;
            if self.a != 0xfa {
                let _o: u8 = 0xfe;
                self.c = (self.a >= _o) as u8;
                self.z = (self.a == _o) as u8;
                self.n = self.a.wrapping_sub(_o) >> 7;
                if self.a != 0xfe {
                    let _o: u8 = 0xff;
                    self.c = (self.a >= _o) as u8;
                    self.z = (self.a == _o) as u8;
                    self.n = self.a.wrapping_sub(_o) >> 7;
                    if self.a != 0xff {
                        let _o: u8 = 0xfd;
                        self.c = (self.a >= _o) as u8;
                        self.z = (self.a == _o) as u8;
                        self.n = self.a.wrapping_sub(_o) >> 7;
                        if self.a != 0xfd {
                            let _o: u8 = 0xfc;
                            self.c = (self.a >= _o) as u8;
                            self.z = (self.a == _o) as u8;
                            self.n = self.a.wrapping_sub(_o) >> 7;
                            if self.a != 0xfc {
                                let _o: u8 = 0xf9;
                                self.c = (self.a >= _o) as u8;
                                self.z = (self.a == _o) as u8;
                                self.n = self.a.wrapping_sub(_o) >> 7;
                                if self.a != 0xf9 {
                                    let _o: u8 = 0xf8;
                                    self.c = (self.a >= _o) as u8;
                                    self.z = (self.a == _o) as u8;
                                    self.n = self.a.wrapping_sub(_o) >> 7;
                                    if self.a != 0xf8 {
                                        let _o: u8 = 0xf7;
                                        self.c = (self.a >= _o) as u8;
                                        self.z = (self.a == _o) as u8;
                                        self.n = self.a.wrapping_sub(_o) >> 7;
                                        if self.a != 0xf7 {
                                            let _o: u8 = 0xf6;
                                            self.c = (self.a >= _o) as u8;
                                            self.z = (self.a == _o) as u8;
                                            self.n = self.a.wrapping_sub(_o) >> 7;
                                            if self.a != 0xf6 {
                                                let _o: u8 = 0xf5;
                                                self.c = (self.a >= _o) as u8;
                                                self.z = (self.a == _o) as u8;
                                                self.n = self.a.wrapping_sub(_o) >> 7;
                                                if self.a != 0xf5 {
                                                    let _o: u8 = 0xf4;
                                                    self.c = (self.a >= _o) as u8;
                                                    self.z = (self.a == _o) as u8;
                                                    self.n = self.a.wrapping_sub(_o) >> 7;
                                                    if self.a != 0xf4 {
                                                        let _o: u8 = 0xf2;
                                                        self.c = (self.a >= _o) as u8;
                                                        self.z = (self.a == _o) as u8;
                                                        self.n = self.a.wrapping_sub(_o) >> 7;
                                                        if self.a != 0xf2 {
                                                            let _o: u8 = 0xf1;
                                                            self.c = (self.a >= _o) as u8;
                                                            self.z = (self.a == _o) as u8;
                                                            self.n = self.a.wrapping_sub(_o) >> 7;
                                                            if self.a != 0xf1 {
                                                                let _o: u8 = 0xf3;
                                                                self.c = (self.a >= _o) as u8;
                                                                self.z = (self.a == _o) as u8;
                                                                self.n = self.a.wrapping_sub(_o) >> 7;
                                                                if self.a != 0xf3 {
                                                                    self.ram[sym::CharPosn] = self.a;
                                                                    return;
                                                                }
                                                                self.getseq();
                                                                let _o: u8 = 0x01;
                                                                self.c = (self.a >= _o) as u8;
                                                                self.z = (self.a == _o) as u8;
                                                                self.n = self.a.wrapping_sub(_o) >> 7;
                                                                if self.a == 0x01 {
                                                                    self.potioneffect();
                                                                }
                                                                // TODO(pass4): lower GotoStmt
                                                            } else {
                                                                self.GoneUpstairs();
                                                                // TODO(pass4): lower GotoStmt
                                                            }
                                                        } else {
                                                            self.getseq();
                                                            let _o: u8 = 0x00;
                                                            self.c = (self.a >= _o) as u8;
                                                            self.z = (self.a == _o) as u8;
                                                            self.n = self.a.wrapping_sub(_o) >> 7;
                                                            if self.a != 0x00 {
                                                                'b29: {
                                                                    let _o: u8 = 0x01;
                                                                    self.c = (self.a >= _o) as u8;
                                                                    self.z = (self.a == _o) as u8;
                                                                    self.n = self.a.wrapping_sub(_o) >> 7;
                                                                    if self.a != 0x01 {
                                                                        let _o: u8 = 0x02;
                                                                        self.c = (self.a >= _o) as u8;
                                                                        self.z = (self.a == _o) as u8;
                                                                        self.n = self.a.wrapping_sub(_o) >> 7;
                                                                        if self.a == 0x02 {
                                                                            self.a = 0x0d;
                                                                            if self.a != 0x00 {
                                                                                break 'b29;
                                                                            }
                                                                        }
                                                                        // TODO(pass4): lower GotoStmt
                                                                    } else {
                                                                        self.a = 0x09;
                                                                    }
                                                                }
                                                                self.addsound();
                                                            }
                                                            self.a = 0x01;
                                                            self.ram[sym::alertguard] = self.a;
                                                            // TODO(pass4): lower GotoStmt
                                                        }
                                                    } else {
                                                        self.a = 0xff;
                                                        self.ram[sym::jarabove] = self.a;
                                                        // TODO(pass4): lower GotoStmt
                                                    }
                                                } else {
                                                    self.a = 0x01;
                                                    self.ram[sym::jarabove] = self.a;
                                                    // TODO(pass4): lower GotoStmt
                                                }
                                            } else {
                                                // TODO(pass4): lower GotoStmt
                                            }
                                        } else {
                                            self.a = self.ram[sym::weightless];
                                            if self.a == 0x00 {
                                                self.getseq();
                                                self.getseq();
                                                // TODO(pass4): lower GotoStmt
                                            }
                                        }
                                    } else {
                                        self.getseq();
                                        self.ram[sym::CharXVel] = self.a;
                                        self.getseq();
                                        self.ram[sym::CharYVel] = self.a;
                                        // TODO(pass4): lower GotoStmt
                                    }
                                } else {
                                    self.getseq();
                                    self.ram[sym::CharAction] = self.a;
                                    // TODO(pass4): lower GotoStmt
                                }
                            } else {
                                self.ram[sym::CharBlockY] = self.ram[sym::CharBlockY].wrapping_add(1);
                                self.addslicers();
                                // TODO(pass4): lower GotoStmt
                            }
                        } else {
                            self.ram[sym::CharBlockY] = self.ram[sym::CharBlockY].wrapping_sub(1);
                            self.addslicers();
                            // TODO(pass4): lower GotoStmt
                        }
                    }
                    self.getseq();
                    let tmp0 = self.a;
                    self.getseq();
                    self.ram[sym::CharSeq + 1] = self.a;
                    self.a = tmp0;
                    self.ram[sym::CharSeq] = self.a;
                    // TODO(pass4): lower GotoStmt
                } else {
                    self.a = self.ram[sym::CharFace];
                    self.a ^= 0xff;
                    self.ram[sym::CharFace] = self.a;
                    // TODO(pass4): lower GotoStmt
                }
            } else {
                self.getseq();
                self.c = 0;
                let _r = (self.a as u16) + self.ram[sym::CharY] as u16 + (self.c as u16);
                self.a = _r as u8;
                self.c = (_r >> 8) as u8;
                self.ram[sym::CharY] = self.a;
                // TODO(pass4): lower GotoStmt
            }
        } else {
            self.getseq();
            self.addcharx();
            self.ram[sym::CharX] = self.a;
            // TODO(pass4): lower GotoStmt
        }
    }

    fn GoneUpstairs(&mut self) {
        self.a = self.ram[sym::level];
        if self.a != 0x0d {
            'b4: {
                if self.a == 0x04 {
                    self.a = 0x06;
                    if self.a != 0x00 {
                        break 'b4;
                    }
                }
                self.a = 0x09;
            }
            self.x = 0x19;
            self.cuesong();
        }
        self.ram[sym::NextLevel] = self.ram[sym::NextLevel].wrapping_add(1);
        return;
    }

    fn CHECKSLICE(&mut self) {
        self.a = self.ram[sym::CharBlockY];
        self.ram[sym::tempblocky] = self.a;
        self.x = 0x09;
        // TODO(pass4): lower LabelStmt
        self.ram[sym::tempblockx] = self.x;
        self.a = self.ram[sym::CDthisframe + self.x as usize];
        let _o: u8 = 0xff;
        self.c = (self.a >= _o) as u8;
        self.z = (self.a == _o) as u8;
        self.n = self.a.wrapping_sub(_o) >> 7;
        if self.a != 0xff {
            // TODO(pass4): lower GotoStmt
        }
        self.a = self.ram[sym::SNthisframe + self.x as usize];
        self.ram[sym::tempscrn] = self.a;
        self.rdblock1();
        let _o: u8 = 0x12;
        self.c = (self.a >= _o) as u8;
        self.z = (self.a == _o) as u8;
        self.n = self.a.wrapping_sub(_o) >> 7;
        if self.a != 0x12 {
            // TODO(pass4): lower GotoStmt
        }
        self.a = self.ram[(self.ram[sym::BlueSpec] as usize | (self.ram[sym::BlueSpec + 1] as usize) << 8) + self.y as usize];
        self.a &= 0x7f;
        let _o: u8 = 0x02;
        self.c = (self.a >= _o) as u8;
        self.z = (self.a == _o) as u8;
        self.n = self.a.wrapping_sub(_o) >> 7;
        if self.a == 0x02 {
            // TODO(pass4): lower GotoStmt
        }
        // TODO(pass4): lower LabelStmt
        self.x = self.ram[sym::tempblockx];
        self.x = self.x.wrapping_sub(1);
        if (self.x as i8) >= 0 {
            // TODO(pass4): lower GotoStmt
        }
        // TODO(pass4): lower LabelStmt
        return;
        // TODO(pass4): lower LabelStmt
        // TODO(pass4): lower LabelStmt
        self.a = self.ram[(self.ram[sym::BlueSpec] as usize | (self.ram[sym::BlueSpec + 1] as usize) << 8) + self.y as usize];
        self.a |= 0x80;
        self.ram[(self.ram[sym::BlueSpec] as usize | (self.ram[sym::BlueSpec + 1] as usize) << 8) + self.y as usize] = self.a;
        // TODO(pass4): lower LabelStmt
        self.a = self.ram[sym::CharPosn];
        let _o: u8 = 0xb2;
        self.c = (self.a >= _o) as u8;
        self.z = (self.a == _o) as u8;
        self.n = self.a.wrapping_sub(_o) >> 7;
        if self.a == 0xb2 {
            // TODO(pass4): lower GotoStmt
        }
        self.a = self.ram[sym::tempblockx];
        self.getblockej();
        self.c = 0;
        let _r = (self.a as u16) + (0x07) as u16 + (self.c as u16);
        self.a = _r as u8;
        self.c = (_r >> 8) as u8;
        self.ram[sym::CharX] = self.a;
        self.a = 0x08;
        self.addcharx();
        self.ram[sym::CharX] = self.a;
        self.x = self.ram[sym::CharBlockY];
        self.x = self.x.wrapping_add(1);
        self.ram[sym::CharY] = self.ram[sym::FloorY + self.x as usize];
        self.a = 0x64;
        self.decstr();
        self.a = 0x05;
        self.addsound();
        self.a = 0x36;
        self.jumpseq();
        self.animchar();
        return;
    }

    fn CHECKSLICE2(&mut self) {
        self.getunderft();
        self.:slice?();
        // TODO(pass4): lower RawIfStmt
        self.ram[sym::tempblockx] = self.ram[sym::tempblockx].wrapping_add(1);
        self.rdblock1();
        if self.a == 0x12 {
            self.a = self.ram[(self.ram[sym::BlueSpec] as usize | (self.ram[sym::BlueSpec + 1] as usize) << 8) + self.y as usize];
            self.a &= 0x7f;
            if self.a == 0x02 {
                self.a = self.ram[sym::tempblockx];
                self.getblockej();
                self.c = 0;
                let _r = (self.a as u16) + (0x07) as u16 + (self.c as u16);
                self.a = _r as u8;
                self.c = (_r >> 8) as u8;
                self.ram[sym::blockedge] = self.a;
                self.a = self.ram[sym::tempscrn];
                self.x = self.ram[sym::tempblockx];
                self.y = self.ram[sym::tempblocky];
                self.getleftbar();
                if self.a < self.ram[sym::CDRightEj] {
                    self.a = self.ram[sym::tempscrn];
                    self.x = self.ram[sym::tempblockx];
                    self.y = self.ram[sym::tempblocky];
                    self.getrightbar();
                    let _o: u8 = self.ram[sym::CDLeftEj];
                    self.c = (self.a >= _o) as u8;
                    self.z = (self.a == _o) as u8;
                    self.n = self.a.wrapping_sub(_o) >> 7;
                    if self.a >= self.ram[sym::CDLeftEj] {
                        // TODO(pass4): lower RawIfStmt
                    }
                }
            }
        }
        return;
    }

    fn CHECKGATE(&mut self) {
        self.a = self.ram[sym::CharAction];
        if self.a != 0x07 {
            self.a = self.ram[sym::CharPosn];
            if self.a != 0x0f {
                if self.a < 0x6c {
                    return;
                }
                if self.a >= 0x6f {
                    return;
                }
            }
        }
        self.getunderft();
        if self.a != 0x04 {
            self.ram[sym::tempblockx] = self.ram[sym::tempblockx].wrapping_sub(1);
            self.rdblock1();
            if self.a != 0x04 {
                return;
            }
        }
        self.x = self.ram[sym::tempblockx];
        self.a = self.ram[sym::CDthisframe + self.x as usize];
        self.a &= self.ram[sym::CDlastframe + self.x as usize];
        if self.a != 0xff {
            return;
        }
        self.gatebarr?();
        // TODO(pass4): lower RawIfStmt
        'b12: {
            'b11: {
                self.BumpSound();
                self.a = self.ram[sym::tempblockx];
                self.ram[sym::collX] = self.a;
                self.getunderft();
                self.a = self.ram[sym::tempblockx];
                let _o: u8 = self.ram[sym::collX];
                self.c = (self.a >= _o) as u8;
                self.z = (self.a == _o) as u8;
                self.n = self.a.wrapping_sub(_o) >> 7;
                if self.a != self.ram[sym::collX] {
                    // TODO(pass4): lower RawIfStmt
                }
                self.a = 0xfb;
                if self.a != 0x00 {
                    break 'b12;
                }
            }
            self.a = 0x05;
        }
        self.c = 0;
        let _r = (self.a as u16) + self.ram[sym::CharX] as u16 + (self.c as u16);
        self.a = _r as u8;
        self.c = (_r >> 8) as u8;
        self.ram[sym::CharX] = self.a;
        return;
    }

    fn gatebarr?(&mut self) {
        self.a = self.ram[(self.ram[sym::BlueSpec] as usize | (self.ram[sym::BlueSpec + 1] as usize) << 8) + self.y as usize];
        self.c = self.a & 1;
        self.a = self.a.wrapping_shr(1);
        self.c = self.a & 1;
        self.a = self.a.wrapping_shr(1);
        self.c = 0;
        let _r = (self.a as u16) + (0x06) as u16 + (self.c as u16);
        self.a = _r as u8;
        self.c = (_r >> 8) as u8;
        let _o: u8 = self.ram[sym::imheight];
        self.c = (self.a >= _o) as u8;
        self.z = (self.a == _o) as u8;
        self.n = self.a.wrapping_sub(_o) >> 7;
        return;
    }

    fn ENEMYCOLL(&mut self) {
        self.a = self.ram[sym::AMtimer];
        if self.a != 0x00 {
            return;
        }
        self.a = self.ram[sym::CharAction];
        let _o: u8 = 0x01;
        self.c = (self.a >= _o) as u8;
        self.z = (self.a == _o) as u8;
        self.n = self.a.wrapping_sub(_o) >> 7;
        if self.a != 0x01 {
            return;
        }
        self.a = self.ram[sym::CharLife];
        if (self.a as i8) >= 0 {
            return;
        }
        self.a = self.ram[sym::CharSword];
        let _o: u8 = 0x02;
        self.c = (self.a >= _o) as u8;
        self.z = (self.a == _o) as u8;
        self.n = self.a.wrapping_sub(_o) >> 7;
        if self.a < 0x02 {
            return;
        }
        'b13: {
            self.getunderft();
            let _o: u8 = 0x14;
            self.c = (self.a >= _o) as u8;
            self.z = (self.a == _o) as u8;
            self.n = self.a.wrapping_sub(_o) >> 7;
            if self.a != 0x14 {
                let _o: u8 = 0x07;
                self.c = (self.a >= _o) as u8;
                self.z = (self.a == _o) as u8;
                self.n = self.a.wrapping_sub(_o) >> 7;
                if self.a != 0x07 {
                    let _o: u8 = 0x04;
                    self.c = (self.a >= _o) as u8;
                    self.z = (self.a == _o) as u8;
                    self.n = self.a.wrapping_sub(_o) >> 7;
                    if self.a == 0x04 {
                        self.gatebarr?();
                        // TODO(pass4): lower RawIfStmt
                    }
                    self.a = self.ram[sym::CharFace];
                    if (self.a as i8) < 0 {
                        return;
                    }
                    self.ram[sym::tempblockx] = self.ram[sym::tempblockx].wrapping_sub(1);
                    self.rdblock1();
                    let _o: u8 = 0x07;
                    self.c = (self.a >= _o) as u8;
                    self.z = (self.a == _o) as u8;
                    self.n = self.a.wrapping_sub(_o) >> 7;
                    if self.a != 0x07 {
                        let _o: u8 = 0x04;
                        self.c = (self.a >= _o) as u8;
                        self.z = (self.a == _o) as u8;
                        self.n = self.a.wrapping_sub(_o) >> 7;
                        if self.a != 0x04 {
                            return;
                        }
                        self.gatebarr?();
                        // TODO(pass4): lower RawIfStmt
                    }
                }
            }
        }
        self.setupchar();
        self.getedges();
        self.a = self.ram[sym::tempscrn];
        self.x = self.ram[sym::tempblockx];
        self.y = self.ram[sym::tempblocky];
        self.rdblock();
        self.ram[sym::tempobjid] = self.a;
        self.checkcoll();
        // TODO(pass4): lower RawIfStmt
        self.DBarr2();
        self.x = self.a;
        if (self.x as i8) >= 0 {
            return;
        }
        self.a ^= 0xff;
        self.c = 0;
        let _r = (self.a as u16) + (0x01) as u16 + (self.c as u16);
        self.a = _r as u8;
        self.c = (_r >> 8) as u8;
        self.addcharx();
        self.ram[sym::CharX] = self.a;
        self.a = 0x41;
        self.jumpseq();
        self.animchar();
        self.rereadblocks();
        return;
    }

    fn DBarr2(&mut self) {
        self.a = self.ram[sym::CharFace];
        if (self.a as i8) >= 0 {
            self.a = self.ram[sym::tempobjid];
            self.cmpbarr();
            // TODO(pass4): lower RawIfStmt
        } else {
            self.a = self.ram[sym::tempobjid];
            self.cmpbarr();
            // TODO(pass4): lower RawIfStmt
        }
        self.a = 0xff;
        return;
    }

    fn :slice?(&mut self) {
        if self.a == 0x12 {
            self.a = self.ram[(self.ram[sym::BlueSpec] as usize | (self.ram[sym::BlueSpec + 1] as usize) << 8) + self.y as usize];
            self.a &= 0x7f;
            if self.a == 0x02 {
                self.a = self.ram[sym::tempblockx];
                self.getblockej();
                self.c = 0;
                let _r = (self.a as u16) + (0x07) as u16 + (self.c as u16);
                self.a = _r as u8;
                self.c = (_r >> 8) as u8;
                self.ram[sym::blockedge] = self.a;
                self.a = self.ram[sym::tempscrn];
                self.x = self.ram[sym::tempblockx];
                self.y = self.ram[sym::tempblocky];
                self.getleftbar();
                if self.a < self.ram[sym::CDRightEj] {
                    self.a = self.ram[sym::tempscrn];
                    self.x = self.ram[sym::tempblockx];
                    self.y = self.ram[sym::tempblocky];
                    self.getrightbar();
                    let _o: u8 = self.ram[sym::CDLeftEj];
                    self.c = (self.a >= _o) as u8;
                    self.z = (self.a == _o) as u8;
                    self.n = self.a.wrapping_sub(_o) >> 7;
                    if self.a >= self.ram[sym::CDLeftEj] {
                        // TODO(pass4): lower RawIfStmt
                    }
                }
            }
        }
        self.c = 0;
        return;
    }

    // aliases: ]slice
    fn :slice(&mut self) {
        self.a = self.ram[(self.ram[sym::BlueSpec] as usize | (self.ram[sym::BlueSpec + 1] as usize) << 8) + self.y as usize];
        self.a |= 0x80;
        self.ram[(self.ram[sym::BlueSpec] as usize | (self.ram[sym::BlueSpec + 1] as usize) << 8) + self.y as usize] = self.a;
        self.a = self.ram[sym::CharPosn];
        if self.a == 0xb2 {
            return;
        }
        self.a = self.ram[sym::tempblockx];
        self.getblockej();
        self.c = 0;
        let _r = (self.a as u16) + (0x07) as u16 + (self.c as u16);
        self.a = _r as u8;
        self.c = (_r >> 8) as u8;
        self.ram[sym::CharX] = self.a;
        self.a = 0x08;
        self.addcharx();
        self.ram[sym::CharX] = self.a;
        self.x = self.ram[sym::CharBlockY];
        self.x = self.x.wrapping_add(1);
        self.ram[sym::CharY] = self.ram[sym::FloorY + self.x as usize];
        self.a = 0x64;
        self.decstr();
        self.a = 0x05;
        self.addsound();
        self.a = 0x36;
        self.jumpseq();
        self.animchar();
        return;
    }
}
